# AL'PAKI LEGEND

Сайт за концептом `ALPAKI_LEGEND_site_concept.pdf`. Зображення замінені на заглушки.

```powershell
cd web
npm install
npm run dev
```

Відкрийте [http://localhost:5173](http://localhost:5173).
