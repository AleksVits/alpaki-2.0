// Replace this demonstration number with the verified sales number before launch.
export const contactPhone = '+380000000000'
export const phoneHref = `tel:${contactPhone}`
export const whatsappHref = `https://wa.me/${contactPhone.slice(1)}`
export const viberHref = `viber://chat?number=${encodeURIComponent(contactPhone)}`
export const emailHref = 'mailto:info@alpaki.ua'
